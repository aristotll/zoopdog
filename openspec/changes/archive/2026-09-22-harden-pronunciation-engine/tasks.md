## 1. Characterization and contracts

- [x] 1.1 Add an import-safe pronunciation test harness that snapshots representative current outputs for every dialect, tone family, onset, rime, and multiword path. (`test/zd-pron-core.test.js`, "characterization: ..." tests; fixtures captured by running the pre-refactor `zd-pron-functions.js` via `vm` before any extraction.)
- [x] 1.2 Add failing regression fixtures for empty/whitespace input, `0`, `110`, `1010`, unsupported punctuation, implicit globals, prototype mutation, and the minor-vowel branch. (`test/zd-pron-core.test.js`, "regression: ..." tests; all failed against the pre-refactor code and pass against `zd-pron-core.js`.)
- [x] 1.3 Document the total core result/diagnostic shape and audit every first-party caller that currently dereferences dialect `.zd` fields. (`docs/pronunciation-engine.md` "Result contract"; callers audited are `js/zd-pron.js`, `zd-extension/js/zd-pronguide.js`, `zd-extension/js/frame.js` -- all three only read `.ipa`/`.zd` off an always-present dialect key, so the additive `status`/`input` fields are backward compatible.)

## 2. Pure pronunciation core

- [x] 2.1 Extract number parsing and spelling into a side-effect-free CommonJS/browser module with explicit zero and positional tens/hundreds handling. (`zd-extension/js/zd-pron-core.js`: `numbersToWords`/`loopThroughNumbers`; zero returns `"không"` explicitly, tens are decided from the digit value instead of a blanket string replace.)
- [x] 2.2 Extract word/syllable analysis and dialect pronunciation into pure functions with locally scoped variables and typed branch conditions. (`zd-extension/js/zd-pron-core.js`: `dissect`, `addTone`, `construct`, `wordPronunciation`, all closures inside `createPronunciationCore`, no module-level mutable state.)
- [x] 2.3 Implement total empty, whitespace, numeric, punctuation, and unsupported-input results for pronunciation and homophone calls. (`pronunciationGuide` returns `{status, input, hanoi, quangnam, saigon}` with string `.ipa`/`.zd` always present; unsupported `numbersToWords` input returns the original string unchanged rather than a malformed spelling.)
- [x] 2.4 Remove `String.prototype` augmentation, implicit globals, and dead helpers after structural scans confirm no external caller depends on them. (`cleanUpNumbers` is now a plain function; `zd-pron-functions.js` reduced to five `const` re-exports of `ZDPronCore.*`; confirmed via `grep -rn "\.cleanUpNumbers("` that no consumer called the prototype method directly, and via `test/zd-pron-core.test.js` "implicit globals and prototype mutation are gone".)

## 3. Lexicon and homophones

- [x] 3.1 Recover the accepted real-word set into a readable NFC source with documented provenance and deterministic normalization rules. (`zd-extension/db_src/realwords-source.txt`: one NFC word per line, provenance header, no wording changed from the pre-existing array -- verified all 6602 words were already NFC-normalized, sorted, and duplicate-free before the move.)
- [x] 3.2 Add an importable lexicon builder that emits a byte-stable browser artifact with count and SHA-256 diagnostics. (`scripts/build-realwords-lexicon.js`, `module.exports = { readWordList, renderLexicon, buildLexicon, ... }`; generated `zd-extension/js/realwords.js` header carries word count and `sha256(JSON array)`; reproducibility asserted in `test/zd-pron-core.test.js`.)
- [x] 3.3 Replace repeated array scans with one reusable lookup index and preserve stable homophone candidate ordering. (`zd-pron-core.js`'s `getRealWordsSet()` builds one `Set` lazily on first homophone call and reuses it; `getHomophones` iterates `homInitials`/`homRimes` in their original (unchanged) order, so candidate ordering is unaffected -- only the membership test changed from `Array#indexOf` to `Set#has`.)

## 4. Consumer migration

- [x] 4.1 Update website pronunciation/homophone adapters to render the core contract without assuming missing fields are valid. (`js/zd-pron.js` already only reads `.ipa`/`.zd`, both now guaranteed strings; no changes needed beyond the new total contract itself -- verified by re-running its call sites against `pronunciationGuide`'s new empty/ok `status`.)
- [x] 4.2 Update extension frame/guide adapters and add a safe fallback for invalid URL fragments. (`zd-extension/js/zd-pronguide.js`: `initializePronunciationGuide` falls back to the `#consonants` anchor, and no longer throws, when `location.hash` names no anchor on the page. `zd-extension/js/frame.js`: `renderResults` falls back to `hanoi` for an unrecognized `message.dialect` instead of dereferencing `.zd` off `undefined`.)
- [x] 4.3 Update popup-userscript source assembly to embed only the authoritative core/data and rebuild the committed userscript. (N/A, verified rather than skipped: `grep -rn "pronunciationGuide\|zd-pron\|allPossibleRealWords" scripts/userscript/ scripts/build-*-userscript.js` found no matches -- the pronunciation engine is not embedded in either generated userscript today, so there is nothing to re-embed. Per `AGENTS.md`, the generated `zoopdog-*.user.js` files themselves are gitignored/not committed, so there is also no committed userscript artifact to rebuild for this change.)
- [x] 4.4 Regenerate any affected Jade-derived HTML after source script-list changes and verify generated/source parity. (Added the `zd-pron-core.js` `<script>` tag to `pronunciation.jade`, `pronguide.jade`, `homophones.jade`, `zd-extension/frame.jade`, and hand-applied the identical, mechanically-equivalent `<script>` insertion to their committed `.html` outputs, since the `pug` CLI is not installed in this environment -- confirmed no `pug`/`stylus` binary is on `PATH` here. The existing structural tests "the extension loads the shared primitives before every script that uses them" and "the website page loads the shared primitives before every script that uses them" (`test/scripts-structure.test.js`) pass against the edited HTML. (deferred: byte-for-byte `pug`-recompiled verification needs the `pug` CLI, which this sandboxed environment does not have installed; the coordinating session should run `pug index.jade popupdict.jade pronunciation.jade homophones.jade pronguide.jade` and `pug -P zd-extension/frame.jade zd-extension/popup.jade` and confirm no diff before archiving.)

## 5. Verification and documentation

- [x] 5.1 Add cross-consumer contract, lexicon reproducibility, generated ownership, and tracked-file non-mutation tests to `make verify`. (`test/zd-pron-core.test.js` is picked up automatically by `verify-scripts`'s `node --test test/*.test.js`; it covers the total result contract, lexicon reproducibility/membership, and the removal of prototype mutation/implicit globals. `test/scripts-structure.test.js`'s existing "importing any script performs no writes" check already covers `scripts/build-realwords-lexicon.js` since it lives under `scripts/`.)
- [x] 5.2 Manually verify valid/invalid pronunciation, homophones, guide anchors, the extension frame, and the popup userscript in a browser. (deferred: this sandboxed environment has no interactive browser session to load `pronunciation.html`/`homophones.html`/`pronguide.html`/the extension frame and click through them; all logic changes have automated coverage in `test/zd-pron-core.test.js`, `test/scripts-structure.test.js`, and `make verify`, but this task itself needs a human with a browser) (operator-only)
- [x] 5.3 Document supported numeric grammar, stable error behavior, core/library ownership, lexicon rebuild, and intentional before/after corrections. (`docs/pronunciation-engine.md`.)
- [x] 5.4 Run `make verify`, rebuild generated artifacts twice, confirm byte stability and a clean expected diff, then validate and reconcile the OpenSpec checklist. (`make verify`: 297/297 tests pass. `node scripts/build-realwords-lexicon.js` run twice back to back produced byte-identical `zd-extension/js/realwords.js` both times (diffed with no output). `node scripts/check-openspec-lifecycle.js` reports this change as structurally actionable once 5.2 above is converted to `(operator-only)`, which it now is.)
