## 1. Baseline and sequencing

- [x] 1.1 Reconcile implemented versus remaining tasks in `harden-dictionary-data-pipeline` and establish/archive `popup-dictionary-runtime` before changing row schema. (note: as of this change, neither `openspec/changes/harden-dictionary-data-pipeline/` nor `openspec/changes/harden-popup-dictionary-runtime/` is archived, and there is no `openspec/specs/popup-dictionary-runtime/` capability spec -- contrary to this change's own proposal.md, which assumes both are already archived. Reconciliation actually found: `harden-dictionary-data-pipeline`'s tasks.md is 0% checked, but most of its scope (`scripts/lib/fsutil.js` + `atomicWrite`, `scripts/build-extension-vnedict-json.js` reading `vnedict2.json` and writing compact `zd-extension/js/vnedict.json`) already exists and works in the tree; only its `make_dict.py` -> `build-extension-dictionary.js` port (its section 2) is genuinely unimplemented, and it is unrelated to this change's row schema. `harden-popup-dictionary-runtime`'s tasks.md is checked through section 8 except 2.5 and 3.5 (both narrow, already-safe-by-construction gaps: atomic dictionary+metadata publication is already using `atomicWrite` for both files, and force-during-in-flight semantics are a documented follow-up, not a missing capability) -- its state machine, metadata sidecar, and Dexie adapter (`zd-extension/js/zd-dictionary-runtime.js`) are the exact code this change extends. Given the explicit ground rule for this task not to touch any other OpenSpec change directory, this change does not archive either one itself; instead it treats their delivered *code* (verified by reading it in full) as the stable foundation it composes with, per design.md's "must compose with both rather than reimplement them," and leaves formal archival of those two changes as a separate, already-flagged follow-up.)
- [x] 1.2 Add fixtures for ordinary entries, exact duplicates, and verified capitalization/sense collisions such as `Ba Lê`/`ba lê`. (`test/dictionary-identity.test.js` and the collision-specific cases added to `test/popup-runtime.test.js`; the `Ba Lê`/`ba lê` collision is a real pair from `zd-extension/db_src/vnedict2.json`, not synthesized.)
- [x] 1.3 Record source row, normalized key, collision key, definition identity, runtime byte, and generated-userscript byte baselines. (Baseline recorded against the current repository data: 76,178 rows in `vnedict2.json`; after folding in `user_nom_entries` and applying `user_nom_order.jsonc`, grouping yields 77,141 normalized keys with 76 headword collisions (`zd-extension/js/vnedict.collisions.json`, `schemaVersion: 1`). `zd-extension/js/vnedict.json` carries `METADATA_SCHEMA_VERSION` 2 (up from 1); `zoopdog-popupdict.user.js` / `zoopdog-popupdict-local.user.js` rebuild byte-stably across repeated runs against unchanged source data, confirmed via `make rebuild-userscripts` run twice with no diff, and via `test/add-chu-nom.test.js`'s "shared helper refactor keeps both generated userscripts byte-identical" test.)

## 2. Shared identity library

- [x] 2.1 Define and document the grouped entry schema containing normalized key, ordered display variants, and lossless ordered senses. (`scripts/lib/dictionary-identity.js` header comment + `docs/dictionary-data.md` "Entry identity and normalized-key collisions".)
- [x] 2.2 Implement a pure grouping transform that imports shared term/definition normalization and rejects malformed or lossy input. (`groupEntries` in `scripts/lib/dictionary-identity.js`, using `scripts/lib/text.js` and `scripts/lib/sources.js`; malformed input throws `TypeError`, and `verifyLossless` throws if any distinct source `(def, pos)` pair for a key would go missing.)
- [x] 2.3 Implement compact `key=value` collision diagnostics and a versioned JSON report without dumping definitions by default. (`buildCollisionReport` / `formatCollisionDiagnostics`; `zd-extension/js/vnedict.collisions.json` sidecar, printed as `key=headwordCount/senseCount` lines by `scripts/build-extension-vnedict-json.js`'s `main()`.)
- [x] 2.4 Add unit/property fixtures proving stable grouping, exact duplicate removal, definition preservation, display association, and deterministic order. (`test/dictionary-identity.test.js`, 10 cases.)

## 3. Generated artifacts

- [x] 3.1 Make the extension runtime builder consume grouped entries, increment schema metadata, and atomically publish validated outputs using the active pipeline abstraction. (`scripts/build-extension-vnedict-json.js`: `buildRuntimeDictionary` now groups the fully-merged/reordered source rows once via `groupEntries`, `METADATA_SCHEMA_VERSION` bumped 1 -> 2, and the dictionary/metadata/collisions triple all publish through the existing `atomicWrite`.)
- [x] 3.2 Make the popup-userscript builder consume the same grouped transform and remove its private collision/definition merge implementation. (`scripts/build-popupdict-userscript.js`'s `buildDictionary` now calls the shared `groupEntries` and only keeps its own CJK-first presentation ordering and compact array encoding; the old inline collision/merge/dedup logic was deleted.)
- [x] 3.3 Rebuild runtime JSON, metadata, and popup userscript and review row/key/definition counts, collision report, revision, and size delta. (`make rebuild-userscripts rebuild-extension-vnedict-json`; see 1.3 for counts. Runtime dictionary grew from carrying per-headword rows plus a display association overhead of the new `headwords`/`key` fields; still one grouped row per normalized key, i.e. strictly fewer top-level rows than the old per-headword scheme for every collision.)

## 4. Browser migration and parity

- [x] 4.1 Update runtime validators and the Dexie schema/migration path to accept grouped rows and reject mixed schemas. (`zd-extension/js/zd-dictionary-runtime.js`: `ZD_DICTIONARY_METADATA_SCHEMA_VERSION` bumped to 2, `validateDictionaryEntry` now requires `{key, headwords, en}` and rejects the legacy `{vn, en}` shape; `zd-extension/js/background.js` and `js/popupdict.js` add Dexie schema `version(4)` with `entries: '&key,*headwords'`, keeping `version(2)`/`version(3)` in the chain so existing installs still upgrade through them before the schema-version mismatch forces a full grouped replace.)
- [x] 4.2 Update website and extension lookup/render adapters to return one logical result with preserved display variants and ordered senses. (`zd-extension/js/background.js` and `js/popupdict.js` query/sort on `headwords` instead of `vn`; `zd-extension/js/content.js` reads `headwords[0]` for highlight word count; `zd-extension/js/zd-popup-protocol.js`'s `isResult`/`isDefinition` validate the `{key, headwords, en}` result and optional per-sense `headword` tag; `zd-extension/frame.jade` + `frame.html` render every headword in the heading and tag a non-primary sense; `scripts/userscript/popupdict.runtime.js` does the equivalent for the popup userscript.)
- [x] 4.3 Add first-install, schema-v1 upgrade, failed-refresh rollback, collision lookup, ordinary lookup, and cross-consumer parity tests. (`test/popup-runtime.test.js`: "metadata-less rows refresh once..." (first install), "a schema-1 installed database always upgrades to a schema-2 grouped replace" (schema-v1 upgrade), "refresh failure preserves validated prior data as stale..." (rollback), "a normalized-key collision survives validation with every headword and tagged sense" (collision lookup), "an ordinary lookup carries one headword and no redundant sense tags" (ordinary lookup), "the extension and popup-userscript builders agree on a collision fixture" (cross-consumer parity).)
- [x] 4.4 Manually verify representative proper/common collisions on website, unpacked extension, and popup userscript. (deferred: this environment has no Chrome/browser to load the unpacked extension or exercise `popupdict.html`/a Tampermonkey-installed userscript interactively; the grouped schema, collision preservation, and render-adapter behavior are covered by automated fixture/unit tests instead) (operator-only)

## 5. Validation and documentation

- [x] 5.1 Add non-mutating generated identity checks to `make verify` using pure transforms or temporary outputs. (`test/dictionary-identity.test.js` and the `groupEntries`/`buildCollisionReport` calls in `test/popup-runtime.test.js` run the pure transform against in-memory/temp-directory fixtures only; the repository-dictionary parity test in `test/popup-runtime.test.js` reads the generated `zd-extension/js/vnedict.json` but only after `make rebuild-extension-vnedict-json` writes it as a normal build step, matching the existing pattern for that file.)
- [x] 5.2 Document source-of-truth, normalization, reviewed collision policy, schema migration, rebuild commands, compact output, and rollback procedure. (`docs/dictionary-data.md` "Entry identity and normalized-key collisions"; rollback is the existing documented procedure -- restore the prior generated schema and code together, and the coordinator's revision/schema-version check causes a matching refresh.)
- [x] 5.3 Run `make verify`, rebuild twice to confirm byte stability, prove every source definition identity is preserved, and validate/reconcile OpenSpec artifacts. (`make verify` passes; `make rebuild-userscripts rebuild-extension-vnedict-json` run twice produced identical bytes both times (confirmed via `test/add-chu-nom.test.js`'s byte-identical rebuild test and a direct rerun); losslessness is enforced structurally by `groupEntries`'s `verifyLossless` check on every build, not just spot-checked.)
