## 1. Shared JavaScript Foundation and CLI Contract

- [x] 1.1 Add `node:test` fixtures that create an isolated repository layout with small `vnedict2.json`, optional `mdx_nom.json`, commented `user_nom_entries.jsonc`, input Markdown, generated outputs, and a controllable injected command runner.
- [x] 1.2 Add characterization tests for current `scripts/user-nom-entries.js` normalization/parsing and byte-identical output from both userscript builders before shared-helper refactoring.
- [x] 1.3 Export the existing text, normalization, JSONC-comment stripping, and text-array helpers from `scripts/user-nom-entries.js`; update both builders to import them without changing generated bytes.
- [x] 1.4 Add failing contract tests for importable `main(argv, io)`, `plan`/`apply` argument parsing, JSON stdout, stderr diagnostics, repository-root override, and stable success/validation/stale/apply-failure exit classes.
- [x] 1.5 Create dependency-free CommonJS `scripts/add-chu-nom.js` with CLI guard/dispatch, version and exit-code constants, repository path resolution, structured result/error helpers, and shared-helper imports until the contract tests pass.

## 2. Deterministic Planning Pipeline

- [x] 2.1 Add tests for default/inline/file source selection, both file-range syntaxes, Markdown filtering, item coordinates, separator handling, and preservation of inline `Vietnamese / ChuNom / explanation` triples.
- [x] 2.2 Implement deterministic input-source parsing and canonical item records until the input tests pass without modifying source files.
- [x] 2.3 Add tests for NFC/whitespace/key normalization, Vietnamese accent folding including `đ`, unique diacritic restoration, ambiguous folded keys, bounded Levenshtein typo suggestions, and stable suggestion ordering.
- [x] 2.4 Implement accent folding, folded indexes, and unresolved correction choices while using the shared normalization helper until tests match representative keys produced by both builders.
- [x] 2.5 Add tests for full-phrase-first expansion, known contiguous subphrase ordering, first-seen de-duplication, existing-user skips, source precedence, CJK extraction, English definitions, inline evidence, and safe Vietnamese-order composition.
- [x] 2.6 Implement single-load local indexes for user entries, `vnedict2.json`, and optional `mdx_nom.json`, then implement deterministic phrase resolution without inferred unsupported values.
- [x] 2.7 Add tests that two identical plans are byte-stable, contain the versioned review/provenance schema and relevant SHA-256 source snapshot, report skipped/unresolved entries, and leave all repository bytes unchanged.
- [x] 2.8 Implement manifest serialization and the `plan` operation until all planning tests pass.
- [x] 2.9 Add failing tests for one-entry-per-line mixed Vietnamese/CJK input, filtering non-Vietnamese-only lines, discarding embedded CJK, and mandatory review metadata.
- [x] 2.10 Implement filtered-input preprocessing and `input-filtered` review status without changing ordinary separator or slash-triple behavior.

## 3. Approval Validation and Source Mutation

- [x] 3.1 Add failing apply-validation tests for missing explicit approval, unknown schema versions, incomplete decisions, unresolved applied entries, invalid Nom/CJK data, duplicate normalized keys, path escapes, and stale dictionary/user/input hashes.
- [x] 3.2 Implement manifest and repository-state validation so every validation/stale failure occurs before any write.
- [x] 3.3 Add JSONC mutation tests covering line/block comments, escaped strings, trailing commas, existing value-only updates, new-entry insertion, indentation/newline preservation, duplicate-key behavior, and post-edit parse validity.
- [x] 3.4 Implement the constrained JSONC lexical scanner and atomic comment-preserving upsert until the JSONC tests pass.
- [x] 3.5 Add file-cleanup tests for whole-line items, one applied item among multiple separators, selected line ranges, and preservation of skipped/rejected/unresolved/unrelated content.
- [x] 3.6 Implement coordinate-based input cleanup and atomic writes until only successfully applied file items are removed.

## 4. Transactional Build and Verification

- [x] 4.1 Add tests with the injected command runner for build order, three Node syntax checks, normalized-key checks in both generated maps, structured success summaries, and rollback after mutation/build/embed/syntax failures.
- [x] 4.2 Implement byte/existence snapshots, workflow-owned-file rollback, existing Node builder invocation, generated-map verification, syntax checks, and the `apply` result contract until transactional tests pass.
- [x] 4.3 Add an isolated end-to-end fixture test that applies reviewed additions and updates, cleans a file item, runs available real Node builders, and proves all owned files are restored when a controlled later stage fails.

## 5. Canonical Codex Command and Claude Reference

- [x] 5.1 Refactor `.codex/commands/add-chu-nom.md` to document exact Node.js `plan` and `apply` invocations, temporary-manifest handling, Vietnamese review presentation, reviewer edits/decisions, and structured-result summary.
- [x] 5.2 Add `.claude/commands/add-chu-nom.md` containing only a heading and a working Markdown reference to `.codex/commands/add-chu-nom.md`.
- [x] 5.3 Remove duplicated mechanical algorithms and direct mutation/build instructions from the canonical Codex command, retaining only input gathering, linguistic review help, approval gating, delegation, and unrelated-worktree safeguards.
- [x] 5.4 Add a focused test that the Claude command resolves to the canonical Codex document and contains no Node.js invocation or duplicated workflow sections.
- [x] 5.5 Verify manually that inline input, a file path, a ranged file mention, edits, rejection, and approval following the canonical instructions delegate all repository reads/writes/builds/checks to `scripts/add-chu-nom.js`.
- [ ] 5.6 Update the canonical review instructions to require dictionary-grounded AI review and explanation completion for `input-filtered` candidates.

## 6. Final Verification

- [x] 6.1 Run `node --test test/add-chu-nom.test.js`, builder characterization tests, and the documented Node syntax checks, recording any unavailable local tooling.
- [x] 6.2 Run `plan` twice against current repository data and compare manifests while confirming `git status --short` shows no plan-induced changes.
- [x] 6.3 Exercise a complete approved apply in an isolated temporary repository, verify both embedded maps and item cleanup, then review the final diff to ensure only shared helpers/builders, the Node.js CLI, tests, the canonical Codex command, the Claude reference document, and intended generated artifacts are affected.
- [x] 6.4 Run a read-only plan for `đích的 thực食` and `đánh打 lạc洛`, verify one candidate per line with `的食` and `打洛` absent, and document the AI-reviewed proposals without applying them.
